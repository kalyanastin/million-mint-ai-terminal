import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Uncomment the line below if you are deploying statically to GoDaddy cPanel */
  // output: 'export',
  // images: { unoptimized: true }
};

export default nextConfig;
